---
- name : ios module
  hosts : cisco
  gather_facts : false
  connection : network_cli
  tasks :
    - name : "Parse text from file"
      ios_command :
        commands :
          - show clock
      register : value
    - debug : var=value.stdout_lines